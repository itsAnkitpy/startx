{{--
    The switches this client company runs on.

    Nothing here names a switch. Every box on the page is built from the declared list in
    code, so a switch a later module adds appears on its own.
--}}
<x-filament-panels::page>
    {{ $this->form }}
</x-filament-panels::page>
